#include <stdio.h>

/**
 * main - print the phrase inside "" followed by new line
 * Return: always return (0)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
